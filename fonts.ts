import {
    Ubuntu,
    Glegoo
} from 'next/font/google';

export const ubuntu = Ubuntu({
    weight: ['400'],
    style: ['normal'],
    subsets: ['latin'],
});

export const glegoo = Glegoo({
    weight: "400",
    subsets: ['latin'],
});